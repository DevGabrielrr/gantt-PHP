RewriteEngine On

# Se a requisição for para um arquivo ou pasta existente, deixa passar
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Senão, redireciona para o index.php
RewriteRule ^ index.php [QSA,L]
